<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
  <permission
    android:name="com.mehmetcerdik.ownerai.permission.BRIDGE"
    android:protectionLevel="signature" />
  <uses-permission android:name="com.mehmetcerdik.ownerai.permission.BRIDGE" />

  <queries>
    <package android:name="com.codespaceapps.aichat" />
    <package android:name="com.mehmetcerdik.ownerbridge" />
    <provider android:authorities="com.mehmetcerdik.ownerbridge.control" />
  </queries>

  <application
    android:allowBackup="false"
    android:label="@string/app_name"
    android:supportsRtl="true"
    android:theme="@style/Theme.GPTAsistan">
    <activity android:name=".MainActivity" android:exported="true">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />
        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <activity
      android:name="com.mehmetcerdik.ownerai.OwnerControlPlaneActivity"
      android:exported="false" />

    <service
      android:name=".PhoneAgentAccessibilityService"
      android:exported="true"
      android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
      <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
      </intent-filter>
      <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
    </service>
  </application>
</manifest>
