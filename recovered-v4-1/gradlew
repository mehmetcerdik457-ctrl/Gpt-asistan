#!/bin/sh
APP_HOME=$(cd "${0%/*}" >/dev/null 2>&1 && pwd -P) || exit 1
CLASSPATH=$APP_HOME/gradle/wrapper/gradle-wrapper.jar
if [ ! -f "$CLASSPATH" ]; then echo "ERROR: gradle-wrapper.jar missing" >&2; exit 20; fi
exec java -Xmx64m -Xms64m -Dorg.gradle.appname=gradlew -classpath "$CLASSPATH" org.gradle.wrapper.GradleWrapperMain "$@"
