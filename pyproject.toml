[project]
name = "gpt-asistan"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "fastapi>=0.115",
  "uvicorn>=0.30",
  "httpx>=0.27",
  "pydantic>=2.8"
]

[project.scripts]
ga = "gpt_asistan.cli:main"

[tool.uvicorn]
factory = false
port = 8000
host = "0.0.0.0"
